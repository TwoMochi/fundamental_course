name: linting, testing, building
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  pipeline:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 17.x ]

    steps:
      - uses: actions/checkout@v2

      - name: Starting Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Build production project
        run: npm run build:prod
        if: always()

      - name: Lint TypeScript
        run: npm run lint:ts
        if: always()

      - name: Lint SCSS
        run: npm run lint:scss
        if: always()

      - name: Run unit tests
        run: npm run test:unit
        if: always()

      - name: Build Storybook
        run: npm run storybook:build
        if: always()

      - name: Run UI tests (Loki)
        run: npm run test:ui:ci
        if: always()

      - name: Generate Loki HTML Report
        if: failure() # Только если тесты упали
        run: npm run test:ui:report

      - name: Upload Loki Report (HTML + Images)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: loki-full-report
          path: .loki # Загружаем всю папку, включая difference, reference, current
